<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

<LinearLayout
        android:orientation="vertical"
        android:background="?attr/background_color"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
     <TextView
             android:layout_width="wrap_content"
             android:layout_height="wrap_content"
             android:layout_margin="10dp"
             android:textStyle="bold"
             android:text="@string/news_papers"
             android:textSize="18sp"
             android:drawableStart="@drawable/ic_assignment_turned_in_black_24dp"
             android:drawableTint="?attr/primary_text_color"
             android:drawablePadding="10dp"
             android:textColor="?attr/primary_text_color"
/>
     <androidx.recyclerview.widget.RecyclerView
             android:layout_margin="10dp"
             app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
             android:id="@+id/news_paper_holder"
             android:scrollbars="vertical"
             tools:itemCount="3"
             tools:listitem="@layout/newspaper"
             android:layout_width="match_parent"
             android:layout_height="wrap_content"/>

     <TextView
             android:layout_width="wrap_content"
             android:layout_height="wrap_content"
             android:layout_margin="10dp"
             android:textStyle="bold"
             android:text="@string/topics"
             android:textSize="18sp"
             android:drawableStart="@drawable/topic_theme"
             android:drawableTint="?attr/primary_text_color"
             android:drawablePadding="10dp"
             android:textColor="?attr/primary_text_color"
             />
    <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_margin="15dp"
            app:cardCornerRadius="5dp"
            android:backgroundTint="@color/white"
            android:layout_height="wrap_content">
     <androidx.appcompat.widget.LinearLayoutCompat
             android:id="@+id/topics_choice_holder"
             android:layout_width="match_parent"
             android:layout_margin="10dp"
             android:layout_height="wrap_content"
             android:padding="10dp"
             android:orientation="vertical"/>
    </com.google.android.material.card.MaterialCardView>
</LinearLayout>
</androidx.core.widget.NestedScrollView>