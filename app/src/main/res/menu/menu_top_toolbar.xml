<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/search"
          android:title="@string/search_title"
          android:icon="@drawable/ic_search"
          android:iconTint="?attr/primary_text_color"
          app:showAsAction="collapseActionView|ifRoom"
          app:actionViewClass="android.widget.SearchView" />
    <item
        android:id="@+id/action_logout"
        android:title="Log Out"
        android:icon="@drawable/log_out"
        android:iconTint="?attr/primary_text_color"
        app:showAsAction="always"/>


    <group android:checkableBehavior="single" >
        <item   android:title="Vertically"
                android:id="@+id/vert_disp"
               android:icon="@drawable/ic_view_day_black_24dp"/>

        <item   android:title="Horizontally"
                android:id="@+id/hori_disp"
                android:icon="@drawable/ic_view_week_black_24dp"
                />
    </group>

    <item android:id="@+id/refresh_categorie"
          android:title="Refresh Categories"
    />

</menu>